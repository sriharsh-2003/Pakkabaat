org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# --- PakkaBaat (local-asr branch) ---
# Bhashini is gone on this branch — ASR runs fully on-device via whisper.cpp instead.
# This key is only a fallback for solo dev testing; the in-app Settings screen (BYOK)
# is what testers actually use, so a shared key here never gets exposed or rate-limited
# across your whole test group. Free, no card required — get from https://aistudio.google.com/apikey
PAKKABAAT_GEMINI_API_KEY=
